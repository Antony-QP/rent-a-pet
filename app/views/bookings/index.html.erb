<div class="container-olivia">
    <h1>My dashboard</h1>

  <% @my_bookings.each do |booking| %>
    <div class="card-dashboard">
      <div class="card-dashboard-image" style="background-image: url('<%= cl_image_path (booking.pet.photo.key)%>')">
      </div>
      <div class="card-dashboard-content">
        <%= booking.pet.name %><br>
        Location: <%= booking.pet.location%><br>
        <%= booking.pet.description %><br>
        Booking date: from <%= booking.start_date %> to <%= booking.end_date %><br>
        <%= booking.pet.price %>€ per day
        <br>
        <br>
        See pet details
      </div>
      <div class="card-dashboard-booking-status">
        <%= booking.status %>
      </div>
      <%= link_to "View Booking", edit_booking_path(booking) %>
    </div>
  <% end %>

<div class="border-status">
</div>

  <% @my_pets_bookings.each do |booking| %>
    <div class="card-dashboard">
      <div class="card-dashboard-image" style="background-image: url('<%= cl_image_path (booking.pet.photo.key)%>')">
      </div>
      <div class="card-dashboard-content">
        <%= booking.pet.name %><br>
        Location: <%= booking.pet.location%><br>
        <%= booking.pet.description %><br>
        Booking date: from <%= booking.start_date %> to <%= booking.end_date %><br>
        <%= booking.pet.price %>€ per day
        <br>
        <br>
        See pet details
      </div>
      <div class="card-dashboard-booking-status">
        <%= booking.status %>
      </div>
      <%= link_to "View Booking Request", edit_booking_path(booking) %>
    </div>
  <% end %>
</div>
